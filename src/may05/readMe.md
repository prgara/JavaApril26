instance var vs local var
parameters vs arguments




Array -> collections of homogeneous elements, fixed size, index based and always starts from 0

nums[1] = 20; assigning a value to arr at position 2 since index starts from 0 not 1
int x = nums[0];   getting the value from array at index 0 position 1 and storing in x variable.          







create an array of 10 elements and get user input to store the values and then print the same on console using loops

WAP to swap 2 nums

a = 10
b = 20


a = 20
b = 10

Wap to reverse an array
input = 1,2,3,4,5
output = 5,4,3,2,1

Take home assignment on arrays :

[10,23,4,5,65,345,27]
- find min and max - 4, 345
- sum of all elements - 
- find even and odd from array
- sort the array in ascending order
- sort the array in descending order




