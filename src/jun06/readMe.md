# Recap
Exception ? Error ? Kind of exception
Checked (Compile) & Unchecked (Runtime)
Parent class of exception ? Throwable
try catch finally throw throws

throw - use when you want to throw an exception explicitly (used inside a method)
throws - when you want to delegate the responsibility of handling the exception to someone else
(used with method signature)

Create a program to check if user is less tha 18 years then you 
will not allow use to book driving test and throw Invalid age exception


try with resource -> try block and no need to close the resource. It will be auto closed.


try with catch
try with catch & finally
try with finally
try with resources

we should force user to handle the exception then use Exception instead of 
RuntimeTimeException class to create a custom exception


=======================================================================================

# Collection

It is a framework which is used to store and manipulate the group of objects. 

It works with object only not with primitive data type.

Types of collection -> list(Arraylist,LinkedList), Set(HashSet,LinkedHashSet), queue, map

- Wrapper classes 
Integer
Char
Double
Boolean
Float



Generics 

Type Casting


Collections
java 8 - lambda, func interfaces, optional, stream api

Spring - xml based config, annotation based config, api, connect with db
MS architecture
Testing
Multithreading - Thread, Runnable, Executor framework, CompletableFuture

Side course - CI/CD - jenkins, Postman(test the REST API), cloud products, DataBase










