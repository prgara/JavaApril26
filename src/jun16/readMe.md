# Recap



LinkedList -> ordered collection, not stored at contiguous memory location , no index

It is a ds where elements are stored in nodes and each node points to next or previous.

node will have 2 objects -> element and memory address
Singly, doubly, circular


search intensive operations use arraylist
frequently add or del operations use linkedlist


create an employee class with id , name & salary. create a list of employees and find employee with the highest salary.


Collections -> utility class
