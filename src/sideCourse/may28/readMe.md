# Recap

what is a branch ?
git branch name
git checkout -b name


A ----- B (made some changes) ---- send these changes to A.


Merge -> Combining changes from one branch to another branch

git merge -> merge th changes from branchName branch to current branch



either stash or commit


stash ->  storing changes at a temporary location and bring them back when needed.


git stash -> stash the changes to temporary location
git stash pop -> bring back the changes from the stash


PR -> Pull request -> request to review the code and merge it.


main ----- create a feature branch ---- make your changes ---- commit and push to github --- raise pr to merge to main


git push -> send changes from local to remote(Github)
git pull -> bring changes from remote to local




Take pull and from main create a feature branch and in student class add subject, section fields 
and update constructor along with getter setters and create PR to merge to main branch.


